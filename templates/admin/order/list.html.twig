{% extends "base.html.twig" %}

{% block title %}Gestion des commandes • Yellow{% endblock %}
{% block body %}
	<main>
		<link rel="stylesheet" href="/css/admin.css">
		<h1>Gestion des commandes</h1>
		<!-- Actions -->
		<div class="actions">
			<!-- Back to catalog link -->
			<a href="{{path('admin')}}" class="back">
				<ion-icon name="chevron-back-outline"></ion-icon>
				Retour à la page d'administration
			</a>
		</div>
		<!-- Orders table -->
		<table>
			<tr>
				<th>Ref de la commande</th>
				<th>ID du client</th>
				<th>Nom du client</th>
				<th>Ref(s) du (des) produit(s)</th>
				<th>Date de la commande</th>
				<th>Montant total</th>
				<th>Actions</th>
			</tr>
			{% for order in orders %}
				<tr>
					<td>{{order.id}}</td>
					<td>{{order.basket.customer.id}}</td>
					<td>{{order.basket.customer.customerFirstName ~ ' ' ~ order.basket.customer.customerLastName}}</td>
					<td>
					{% for content in order.basket.contents %}
					{{content.product.productName}}
					{% endfor %}</td>
					<td>{{order.date}}</td>
					<td>{{order.orderTotal}}</td>
					<td class="actions">
						<a href="#" class="delete">Supprimer</a>
					</td>
				</tr>
			{% endfor %}
		</table>
	</main>
{% endblock %}